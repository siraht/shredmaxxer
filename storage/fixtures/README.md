# Storage Fixtures

These files are small, human-readable datasets for manual checks of:
- v3 -> v4 migration
- v4 merge behavior
- import/replace flows

Notes:
- v3 fixtures use label rosters and segment logs.
- v4 fixtures use roster items with stable ids.
- Date keys are YYYY-MM-DD.
